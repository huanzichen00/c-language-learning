# Project1: 通讯录（标准版）

字段：name, phone, email
功能：新增、删除、修改、查找、列表、保存、加载

## Day1 目标（今天只做一个点）
知识点：结构体数组 + 菜单循环框架

你先完成：
1. 菜单循环（读取命令）
2. `add_contact`（新增）
3. `list_contacts`（列出）

暂时不做：删除/修改/查找/文件读写（后续再加）

## 编译
```bash
gcc -Wall -Wextra -Werror -std=c11 -g contacts.c -o contacts
```

## 运行
```bash
./contacts
```
